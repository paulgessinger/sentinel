apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: merge-sentinel
spec:
  to:
    kind: Service
    name: merge-sentinel
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
